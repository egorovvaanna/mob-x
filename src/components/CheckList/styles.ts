import styled from "styled-components";


export const CheckListWrapper = styled.div(({theme}) => ({
  cursor: "default",
  marginTop: "10px",
}));

export const CompleteOfCheck = styled.div(({theme}) => ({
    display: "flex",
  cursor: "default",
  margin: "10px 0px",
  justifyContent: "end"
}));

